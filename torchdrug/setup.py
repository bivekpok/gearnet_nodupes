from setuptools import setup, find_packages

setup(
    name="torchdrug-custom",  # Unique name to avoid PyPI conflicts
    version="0.1.0",         # Match your modified version
    packages=find_packages(),
)
